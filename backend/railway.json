{
  "build": {
    "builder": "nixpacks",
    "config": {
      "pythonVersion": "3.11"
    }
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyMaxRetries": 5
  }
}
