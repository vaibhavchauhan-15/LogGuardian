# Vercel-compatible requirements — excludes torch and sentence-transformers
# which are optional (code falls back gracefully) and exceed Vercel's 250 MB
# package-size limit. For local dev with full ML capabilities use requirements.txt.
fastapi==0.116.1
uvicorn[standard]==0.35.0
python-multipart==0.0.20
pydantic-settings==2.10.1
supabase==2.7.4
python-dateutil==2.9.0.post0
scikit-learn==1.7.2
joblib==1.5.2
numpy==2.3.3
redis==6.4.0
